pipeline {
 agent any

 stages {

  stage('Checkout') {
   steps {
    git 'https://github.com/vibha555/devops-project.git'
   }
  }

  stage('Build Docker Image') {
   steps {
    sh 'docker build -t vibha-app .'
   }
  }

  stage('Verify Image') {
   steps {
    sh 'docker images'
   }
  }

 }
}
